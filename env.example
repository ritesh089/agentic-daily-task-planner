# ============================================================================
# LangFuse Configuration
# ============================================================================
# Copy this file to .env and update with your values:
#   cp env.example .env
#
# Generate secrets with:
#   openssl rand -base64 32
#
# DO NOT commit .env to git!
# ============================================================================

# NextAuth Secret (generate with: openssl rand -base64 32)
# Required for session management
LANGFUSE_NEXTAUTH_SECRET=changeme-generate-with-openssl-rand-base64-32

# Salt for API key hashing (generate with: openssl rand -base64 32)
# Required for secure API key storage
LANGFUSE_SALT=changeme-generate-with-openssl-rand-base64-32

# Telemetry (opt-out if desired)
LANGFUSE_TELEMETRY_ENABLED=true

# ============================================================================
# LangFuse API Keys (Set AFTER first login)
# ============================================================================
# 1. Start services: docker-compose up -d
# 2. Open http://localhost:3000
# 3. Create an account (any email/password)
# 4. Go to Settings → API Keys
# 5. Create a new API key
# 6. Copy Public Key and Secret Key here
# 7. Restart your application to use the keys
# ============================================================================

# Public Key (starts with pk-lf-...)
LANGFUSE_PUBLIC_KEY=

# Secret Key (starts with sk-lf-...)
LANGFUSE_SECRET_KEY=

# Host URL (default for local development)
LANGFUSE_HOST=http://localhost:3000

# ============================================================================
# Usage in Your Application
# ============================================================================
# Export these variables before running your app:
#   export $(cat .env | xargs)
#   cd examples/conversational-assistant
#   python main.py
#
# Or source them:
#   source .env
#   python main.py
# ============================================================================

